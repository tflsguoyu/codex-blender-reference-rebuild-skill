# Example Prompts

```text
Use $blender-reference-rebuild to reconstruct this bedroom reference image in Blender. Create the screen-space and world-space contracts first, then build and validate the scene.
```

```text
Use $blender-reference-rebuild to audit my latest Blender render against the reference image. Focus on screen-space bbox, crop, perspective lines, and blocking geometry.
```

```text
Use $blender-reference-rebuild to continue this indoor scene reconstruction. The current render is close, but the right window and chair region still differ from the reference.
```

